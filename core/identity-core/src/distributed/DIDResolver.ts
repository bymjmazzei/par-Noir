import { DIDDocument, VerificationMethod } from '../types';
import { DIDValidator } from '../utils/didValidator';

// DID Resolver Types and Interfaces - Now exported from the modular system
export { DIDResolver } from './didResolver/didResolver';

// The DIDResolver class is now exported from the modular DID resolver system 