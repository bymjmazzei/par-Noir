# Static assets

Copy the OAuth callback page from `@par-noir/oauth-ui`:

`packages/oauth-ui/static/oauth-callback.html`

(or `node_modules/@par-noir/oauth-ui/static/oauth-callback.html` when installed via workspace/`file:`).

Register the same path as your OAuth client's `redirect_uri`.
