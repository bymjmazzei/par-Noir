{
  "name": "@par-noir/oauth-ui",
  "version": "1.0.0",
  "description": "Shared OAuth unlock/lock icons and button components for par Noir apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "npm run build:vault-script && tsc",
    "build:vault-script": "esbuild src/cloudVaultBrowser.ts --bundle --format=iife --target=es2017 --outfile=static/oauth-cloud-vault.js --banner:js=\"/* GENERATED from src/cloudVaultBrowser.ts by 'npm run build:vault-script'. Do not edit by hand. */\"",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "peerDependencies": {
    "react": ">=17.0.0"
  },
  "dependencies": {
    "@par-noir/device-cloud-credentials": "file:../device-cloud-credentials",
    "@par-noir/user-owned-storage": "file:../user-owned-storage"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "esbuild": "^0.25.9",
    "react": "^18.2.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "files": [
    "dist",
    "src",
    "static"
  ]
}
