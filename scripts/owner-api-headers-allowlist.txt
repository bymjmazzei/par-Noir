# Burn-down: aggregator-browser files allowed to call ownerApiHeadersAsync outside
# the ownerFetch / messageFetch gates (package buildAuthHeaders callbacks only).
# Entries are only ever REMOVED, never added.
#
# Canonical gates (not listed — always allowed):
#   apps/aggregator-browser/src/services/ownerApiFetch.ts
#   apps/aggregator-browser/src/services/ownerApiHeaders.ts
#   apps/aggregator-browser/src/services/messageAuthFetch.ts
#
# Phase 2 complete: allowlist empty — mailbox/outbox mint in
# @par-noir/device-cloud-credentials; publicShare via ownerFetch.
