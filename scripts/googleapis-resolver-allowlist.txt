# Burn-down list for the googleapis → device-cloud-credentials import rule.
#
# Any in-scope client file that fetches googleapis.com (Drive / OAuth2 token /
# userinfo) must import from @par-noir/device-cloud-credentials so freshness is
# decided by the one resolver. Files listed here still touch Google directly
# without that import; entries are only ever REMOVED, never added.
#
# Paths relative to repo root. Blank lines and # comments ignored.
#
# Phase 4 complete: allowlist empty (comments only).
