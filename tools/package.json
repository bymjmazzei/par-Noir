{
  "name": "@identity-protocol/tools",
  "version": "1.0.0",
  "description": "Tools for identity protocol",
  "scripts": {
    "build": "echo 'Tools build completed'",
    "build:storage": "cd storage-tool && npm run build",
    "build:browser": "cd browser-tool && npm run build",
    "build:monetization": "cd monetization-tool && npm run build",
    "build:security": "cd security-tool && npm run build",
    "test": "echo 'Tools tests completed'",
    "clean": "echo 'Tools clean completed'"
  },
  "private": true
}
