# Copy to .env locally. Do not commit .env.
# Required for production build:
# VITE_API_ENDPOINT=https://your-api-host

# VITE_PN_CLIENT_ID=browser-app

# Optional: browser error reporting (production only; strips query string in beforeSend)
# VITE_SENTRY_DSN=
# VITE_SENTRY_TRACES_SAMPLE_RATE=0
