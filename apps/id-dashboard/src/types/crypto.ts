/** @deprecated Prefer importing these types from `@par-noir/identity-crypto`. */
export type {
  KeyPair,
  DIDResult,
  DIDKeyPair,
  AuthSession,
  IdentityData,
  EncryptedData,
  EncryptedIdentity,
  AuthenticationResult,
  RecoveryKeyData,
  TokenPayload,
  TokenHeader,
  DecryptionParameters,
  IdentityCreationResult,
} from '@par-noir/identity-crypto';
