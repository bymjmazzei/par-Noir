/** Re-export shared IPC contracts from @par-noir/desktop-ipc. */
export {
  NATIVE_IPC_CHANNEL,
  SECURE_VOLUME_IPC_CHANNEL,
  type SecureVolumeIdentity,
  type SecureVolumeMountState,
  type SecureVolumeUnlockPayload,
} from '@par-noir/desktop-ipc';
